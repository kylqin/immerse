export class ReaderConf {
  constructor(
    public theme: string, // background color
    public pageMode: string,
    public pageScale: number,
    public fontFamily: string,
    public fontSize: number,
    public lineHeight: number,
  ) {}
}
